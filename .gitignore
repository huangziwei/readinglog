/target
# Anywhere, not only the root: a tool run from a subdirectory writes its own
# scratch under one there.
artifacts/
.DS_Store
.claude

# Staged by build.sh from target/. Named, never the directory: ignoring `bin/`
# would drop bin/readinglog.sh from the release zip.
/device/extensions/readinglog/bin/readinglog
.github/assets/calendar.png
# Written by hand: the host path READINGLOG_FONTS takes.
/preview.env